import PreviewReceta from "../../../public/preview.svg";

const RecetaDetail = () => {
  return (
    <div style={{
        marginLeft: "10px"
    }}>
      <img  src={PreviewReceta}/>
    </div>
  );
};

export default RecetaDetail;
